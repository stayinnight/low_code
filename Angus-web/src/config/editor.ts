
/*每个位置圆点对应的方向*/
export enum positionConfig {
    left = 'width left',
    leftTop = 'width height left top',
    top = 'height top',
    rightTop = 'width height top',
    right = 'width',
    rightBottom = 'width height',
    bottom = 'height',
    leftBottom = 'height width left',
}

export enum changeProperies {
    left = "left",
    top = "top",
    width = "width",
    height = "height"
}